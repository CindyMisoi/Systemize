databases:
  - name: postgres
    ipAllowList: []
    databaseName: systemize_production
    user: systemize

services:
  - type: web
    name: systemize
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false